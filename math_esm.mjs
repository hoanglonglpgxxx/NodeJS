// export default (a, b) => {
//     return a + b;
// };

// export default add;

const add = (a, b) => {
    return a + b;
};

const sub = (a, b) => {
    return a - b;
};

export default {
    add,
    sub
};
